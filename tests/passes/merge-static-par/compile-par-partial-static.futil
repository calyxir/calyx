// -p validate -p merge-static-par

import "primitives/core.futil";

component main() -> () {
  cells {
    a = std_reg(2);
  }

  wires {
    group A<"static"=1> {
      a.in = 2'd0;
      a.write_en = 1'b1;
      A[done] = a.done;
    }
  }

  control {
    par { A}
  }
}
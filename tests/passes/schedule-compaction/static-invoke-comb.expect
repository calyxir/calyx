import "primitives/core.futil";
import "primitives/binary_operators.futil";
component main(@go go: 1, @clk clk: 1, @reset reset: 1) -> (@done done: 1) {
  cells {
    mult = std_mult_pipe(32);
    add = std_add(32);
    tmp = std_reg(32);
  }
  wires {
    comb group comb_invoke0 {
      add.right = mult.out;
      add.left = 32'd2;
    }
  }
  control {
    static<4> seq  {
      static<3> invoke mult(
        left = 32'd2,
        right = 32'd3
      )();
      static<1> invoke tmp(
        in = add.out
      )() with comb_invoke0;
    }
  }
}

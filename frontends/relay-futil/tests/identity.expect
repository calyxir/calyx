import "primitives/std.lib";
component main() -> () {
  cells {
    x = prim std_reg(32);
    ret0 = prim std_reg(32);
  }
  wires {
    group group0 {
      ret0.in = x.out;
      ret0.write_en = 1'd1;
      group0[done] = ret0.done;
    }
  }
  control {
    seq {
      group0;
    }
  }
}

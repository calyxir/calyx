import "primitives/core.futil";

component main(lhs: 64, rhs: 64) -> (result: 64) {
  cells {
    adder = std_add(64);
  }
  wires {
    adder.left = lhs;
    adder.right = rhs;
    result = adder.out;
  }
  control {}
}

import "primitives/std.lib";

component main() -> () {
  cells {
    const1 = std_const(32, 400);
    sqrt0 = std_sqrt(32);

  }

  wires {

    group op {
      sqrt0.in = const1.out;
      op[done] = sqrt0.done;
    }
    
  }

  control {
    seq {
      op;
    }
  }
}
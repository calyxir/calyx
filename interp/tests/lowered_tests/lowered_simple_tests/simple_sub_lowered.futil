import "primitives/std.lib";
component main<"static"=1>(go: 1, clk: 1) -> (done: 1) {
  cells {
    const0 = std_const(32, 400);
    const1 = std_const(32, 9);
    sub0 = std_sub(32);
    reg0 = std_reg(32);
  }
  wires {
    done = reg0.done ? 1'd1;
    reg0.clk = clk;
    reg0.in = go ? sub0.out;
    reg0.write_en = go ? 1'd1;
    sub0.left = go ? const0.out;
    sub0.right = go ? const1.out;
  }

  control {}
}


// use std::collections::HashMap;

// use super::prelude::*;

// #[derive(Debug, Clone, Default)]
// pub struct Attributes {
//     // We don't need deterministic iteration or global position for the interpreter
//     attrs: HashMap<Identifier, u64>,
// }

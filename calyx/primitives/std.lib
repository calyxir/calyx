
(define/library
  (define/prim (std/const p_width p_value)
    ((port valid 1) (port reset 1))
    ((port out p_width) (port ready 1)))

  (define/prim (std/add p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/sub p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/mul p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/div p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/and p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/or p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out p_width) ))

  (define/prim (std/reg p_width)
    ((port in p_width) (port reset 1) (port valid 1) (port reset 1))
    ((port out p_width) (port ready 1)))

  (define/prim (std/gt p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) ))

  (define/prim (std/lt p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) ))

  (define/prim (std/eq p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) ))

  (define/prim (std/ge p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) ))

  (define/prim (std/le p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) ))

  (define/prim (std/gt p_width)
    ((port in0 p_width) (port in1 p_width))
    ((port out 1) )))

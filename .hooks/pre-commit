#/bin/sh

function check {
    prog=$1
    shift
    "$prog" "$@"
    if [ $? -ne 0 ]; then
        exit 1
    fi
}

check cargo clippy

staged_py_files=$(git diff --cached --name-only --diff-filter=ACM | grep '\.py$')

if [ -n "$staged_py_files" ]; then
    flake=$(which flake8 || which flake)
    if [ -z "$flake" ]; then
        echo "flake is not installed"
        exit 1
    else
        check "$flake" $staged_py_files
    fi
fi

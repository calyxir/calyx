name: Rust

on: [push]

jobs:
  interpreter:
    name: Test Interpreter
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

  compiler:
    name: Test Compiler
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2

